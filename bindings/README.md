# `bindings/` — language & framework bindings

- **Python** via pybind11 (mirrors the C++ core; see the API sketch in [`../docs/DESIGN.md`](../docs/DESIGN.md) §9).
- **ROS2 / Isaac** node wrapping the online `ExtrinsicTracker` and publishing `CameraInfo`.

**Status:** placeholder — no implementation yet.
